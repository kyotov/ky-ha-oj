apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: oj-microline
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: oj-microline
  endpoints:
    - port: http
      path: /metrics/
      interval: 60s
